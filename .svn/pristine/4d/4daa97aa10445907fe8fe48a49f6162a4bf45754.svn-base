<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="role" >
<!-- 	<select id="findUserRoles" resultType="com.evergrande.hdmp.sys.entity.Role" parameterType="string">
		select * from sys_user u 
		left join user_role_rela ur on u.user_id=ur.user_id 
		left join sys_role r on ur.role_id=r.role_id
		where u.username=#{username};
	</select>
	<select id="findRoleList" resultType="com.evergrande.hdmp.sys.entity.Role" >
		select * from sys_role;
	</select>
	<select id="findRoleListByPage" parameterType="hashmap" resultType="com.evergrande.hdmp.sys.entity.Role">
		select * from sys_role limit #{beginIndex},#{pageSize};
	</select>
	<select id="findTotalNum" resultType="integer">
		select count(1) from sys_role;
	</select> -->
</mapper>